# feat : ajoute les tf_idf_processors

Nous avons maintenant mis prêt les services pour calculer :
- le `tf` (term frequency) 
- le `idf` (inverse document frequency)
- le `tf_idf` (term frequency inverse document frequency)

Ces services retournent, tous, le `Term`